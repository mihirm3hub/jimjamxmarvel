{
  "name": "jimjam-marvel-biscuit-detector-webar",
  "private": true,
  "version": "1.0.0",
  "description": "WebAR mini-game with in-browser biscuit detection using ONNX Runtime Web + Web Workers, model served via AWS CloudFront.",
  "scripts": {
    "start": "npx http-server ./web -p 8080 -c-1",
    "start:https": "npx http-server ./web -p 8443 -S -C cert.pem -K key.pem"
  }
}